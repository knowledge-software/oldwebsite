/*This is a console shell whose purpose is to provide a platform totest the calculator code.  It is far more productive to use thisfacility to test the basic code elements outside the plug-in environment.*/#include <iostream>//#include	"Parser.h"#include	"LR1LA.h"using namespace std;  //introduces namespace stdint main(){		const	short	size = 500;	char	lineToBeParsed[size];	char	answer[size];	LR1LA	theCompiler;	cout << "Lexical Parser Console" << endl << endl;	while(1) {		cout << "Enter a line to be parsed: ";				cin.getline(lineToBeParsed, size); 		if ( *lineToBeParsed == '\0' ) break;			cout << "I got Ò" << lineToBeParsed << "Ó ";//		LR1LA	theCompiler( lineToBeParsed );		theCompiler.Compile( lineToBeParsed );		if( !theCompiler.Answer(answer) )			cout << "-> !Error: ";		else			cout << " = ";		cout << answer << endl << endl;				}			return 0;}